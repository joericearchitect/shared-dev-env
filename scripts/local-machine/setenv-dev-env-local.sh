export SCRIPTS_DIR="$(cd "$(dirname "$0")" && pwd)"

# TODO - when setting these export variables, check to see if they already exist on the 

export SITE_JARCH_GIT_PROJECT_DIR=$GIT_PROJECTS_DIR/site-joe-rice-architect
export APP_MCAT_GIT_PROJECT_DIR=$GIT_PROJECTS_DIR/app-mine-the-catalog

export DEV_ENV_VAGRANT_DIR=$PROJ_DIR_SHARED_DEV_ENV/vagrant